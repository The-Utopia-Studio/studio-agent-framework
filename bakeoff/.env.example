# Mock suite: no credentials are required.
# Live Convex re-test only. Never commit a populated .env file.
CONVEX_URL=
CONVEX_ADMIN_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
DIGEST_MODEL=
# Required when the provider host is not moonshot, anthropic, openai, or localhost.
DIGEST_MODEL_HOSTS=
